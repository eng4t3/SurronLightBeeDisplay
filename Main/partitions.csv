# LightBee Display partition table (16 MB flash, two OTA app slots).
# Identical to the core's "16M Flash (3MB APP/9.9MB FATFS)" scheme so that
# NVS stays at 0x9000 and the stored odometer/settings survive the switch.
# The Arduino ESP32 core automatically uses a partitions.csv found in the
# sketch folder. Changing the partition table requires ONE USB flash; every
# update after that can be done over WiFi.
# Name,   Type, SubType, Offset,  Size, Flags
nvs,      data, nvs,     0x9000,  0x5000,
otadata,  data, ota,     0xe000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x300000,
app1,     app,  ota_1,   0x310000,0x300000,
ffat,     data, fat,     0x610000,0x9E0000,
coredump, data, coredump,0xFF0000,0x10000,
